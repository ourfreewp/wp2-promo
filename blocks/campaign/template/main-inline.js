// WP2 Lead Campaign Block Interactivity
document.addEventListener('DOMContentLoaded', function() {
  const block = document.getElementById('wp2-lead-campaign-block');
  if (!block) return;
  // Example: Add event listeners or dynamic rendering here
  // e.g. block.querySelector('.wp2-lead-campaign__actions').innerHTML = '<button>Sign Up</button>';
});
