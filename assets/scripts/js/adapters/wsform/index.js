// WSForm Adapter
// Usage: import and call wsformAdapter methods as needed

export function trackWSFormEvent(eventName, data = {}) {
  // Placeholder for WSForm integration logic
  // For example, trigger analytics or custom events
  // window.WSForm?.trackEvent?.(eventName, data);
}
